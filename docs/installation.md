# Установка

> Требует Python >= 3.9

## Через `pip`

```bash
pip install async-argon2
```

## Через `poetry`

```bash
poetry add install async-argon2
```

## Через `uv`

```bash
uv add install async-argon2
```